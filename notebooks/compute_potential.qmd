---
title: Testing functions
format:
  html:
    embed-resources: true
---

```{r}
devtools::load_all("../model")
```

```{r}
data_dir <- "data/one_hot"
persona <- biodt.recreation::load_persona("persona.csv")

layers <- biodt.recreation::compute_potential(persona, data_dir)

terra::plot(layers)
```

```{r}
terra::plot(layers[["Recreational_Potential"]])
```
